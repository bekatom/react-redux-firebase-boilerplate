// Simple React Native specific changes

export const local = {

};

export default {
    // font scaling override - RN default is on
    // ReactCBLight host
    // localHost: `http://${local.user}:${local.pass}@localhost:${local.port}`,
    // localHost: 'http://localhost:5984',

};
